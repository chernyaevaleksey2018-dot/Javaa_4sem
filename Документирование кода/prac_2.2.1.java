//Утилита javadoc — это стандартный инструмент JDK, который автоматически собирает ваши комментарии вида /** ... */ и превращает их в полноценный веб-сайт с описанием кода. Именно так создается официальная документация Java API.

1. 
/**
 * Класс для математических операций.
 * @author Черняев Алексей
 * @version 1.0
 */
public class Calculator 
{
    /**
     * Складывает два числа.
     * @param a первое число
     * @param b второе число
     * @return сумма чисел
     */
    public int add(int a, int b) 
    {
        return a + b;
    }
}

/* 2. Генерация через командную строку
Открыть терминал в папке с проектом и выполнить команду: 
javadoc -d docs Calculator.java
-d docs: указывает утилите создать папку docs и сложить туда все HTML-файлы.
Calculator.java: имя вашего файла (можно указать название пакета или несколько файлов сразу).
Важный момент: если в комментариях есть русский текст, нужно добавить параметры кодировки: 
javadoc -d docs -encoding UTF-8 -charset UTF-8 Calculator.java

3. 3. Как посмотреть результат.
В папке docs появится множество файлов. Нужно найти и открыть в браузере index.html — это главная страница вашей документации. */
